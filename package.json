{
  "name": "radix-meme-gateway",
  "version": "0.0.5",
  "description": "Utility to fetch relevant data for radix.meme app directly from the radix ledger",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/radix-meme/radix-meme-gateway.git"
  },
  "scripts": {
    "build": "tsup",
    "test": "echo \"Error: no test specified\" && exit 1",
    "repl": "npm run build && clear -x && ( node --experimental-repl-await -i -e \"$(< init-dev-env.js)\" --max-old-space-size=14336)"
  },
  "keywords": [
    "radix"
  ],
  "author": "fliebenberg,dcts",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "axios": "^1.7.7"
  }
}
